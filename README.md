# json2Excel
Script to convert huge JSON dataset(~1GB) into train and test data in Excel format
The JSON dataset in my script is in .gz (zip file) format.
This script will extract zip contents,using pandas it'll create and split the original DataFrame into Test and Train DataFrames
